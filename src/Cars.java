import java.util.Arrays;
import java.util.List;
import java.util.Random;

public class Cars {

    private List<String> cars = Arrays.asList("Automatic", "Manual");

    private Random random = new Random();

    public getNextCar(String brand){
        brand = random.nextInt(Car.getGear)
        return brand;

    }
}
